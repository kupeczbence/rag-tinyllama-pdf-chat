pip install langchain
pip install langchain-community
pip install langchain-core
pip install sentence-transformers
pip install faiss-cpu
pip install pypdf
pip install streamlit
pip install transformers
pip install torch
#Elsőnek az ingest fájlt kell futtatni, hogy létrehozza a vector store-t a PDF dokumentumokból, majd az app.py-t (streamlit run app.py a terminálban), hogy elindítsa a chatbotot.